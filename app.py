import streamlit as st
import pandas as pd
import matplotlib.pyplot as plt
import plotly.express as px

# تحميل البيانات
data = {
    "اسم المدرب": ["إبراهيم جلال", "بدور", "إسراء", "آلاء", "جياد", "حذيفة", "دعاء عينية", "بشاير", "عبد الله", "مصطفى", "حميدي"],
    "الحد الأدنى": [80000, 35000, 40000, 60000, 80000, 80000, 80000, 80000, 80000, 60000, 60000],
    "هدف البيانات": [56000, 38500, 36000, 56000, 56000, 56000, 56000, 56000, 56000, 45000, 45000],
    "سابق البيانات": [81383.07, 20574.8, 13514.08, 68184.95, 12885.84, 22675.64, 22675.64, 22675.64, 22675.64, 56809.13, 56809.13],
    "تحقيق البيانات": [1.02, 0.58, 0.38, 1.22, 0.16, 0.28, 0.28, 0.28, 0.28, 1.26, 1.26],
    "هدف العملاء": [5, 1, 1, 4, 45, 3, 3, 3, 3, 4, 4],
    "عدد العملاء المتدربين": [10, 2, 1, 5, 40, 3, 3, 3, 3, 5, 22],
    "تحقيق العملاء": [2.00, 2.00, 1.00, 1.25, 0.89, 1.00, 1.00, 1.00, 1.00, 1.25, 5.50]
}

df = pd.DataFrame(data)

# عنوان الداشبورد
st.set_page_config(page_title="لوحة أداء المدربين", layout="wide")
st.title("📊 لوحة أداء المدربين")
st.markdown("تحليل شامل لأداء المدربين من حيث البيانات والعملاء")

# KPIs
col1, col2, col3 = st.columns(3)
col1.metric("📈 متوسط تحقيق البيانات", f"{df['تحقيق البيانات'].mean():.2f}")
col2.metric("👥 متوسط تحقيق العملاء", f"{df['تحقيق العملاء'].mean():.2f}")
col3.metric("🔥 أعلى مدرب في العملاء", df.loc[df['تحقيق العملاء'].idxmax(), 'اسم المدرب'])

# رسم بياني: تحقيق البيانات
st.subheader("📊 نسبة تحقيق البيانات لكل مدرب")
fig1 = px.bar(df, x="اسم المدرب", y="تحقيق البيانات", color="تحقيق البيانات", text="تحقيق البيانات",
              color_continuous_scale="Viridis", title="تحقيق البيانات")
st.plotly_chart(fig1, use_container_width=True)

# رسم بياني: تحقيق العملاء
st.subheader("👥 نسبة تحقيق العملاء لكل مدرب")
fig2 = px.bar(df, x="اسم المدرب", y="تحقيق العملاء", color="تحقيق العملاء", text="تحقيق العملاء",
              color_continuous_scale="Blues", title="تحقيق العملاء")
st.plotly_chart(fig2, use_container_width=True)

# جدول البيانات
st.subheader("📋 جدول الأداء الكامل")
st.dataframe(df)

# فلترة حسب المدرب
selected_trainer = st.selectbox("🔍 اختر مدرب لعرض التفاصيل", df["اسم المدرب"])
filtered = df[df["اسم المدرب"] == selected_trainer]
st.write("تفاصيل الأداء:")
st.dataframe(filtered)
